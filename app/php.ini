; Extension pdo_pgsql chargée depuis ~/.local (installée sans sudo via apt-get download).
; À supprimer une fois `sudo apt install php8.4-pgsql` exécuté.
extension=/home/remikel/.local/lib/php/20240924/pdo_pgsql.so
