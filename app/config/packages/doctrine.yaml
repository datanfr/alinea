doctrine:
    dbal:
        # Base « canutes » : open data Légifrance + Assemblée nationale,
        # plus le schéma « alinea » (CRI importés par app:import:comptes-rendus).
        url: '%env(resolve:DATABASE_URL)%'
        server_version: '17'
        profiling_collect_backtrace: '%kernel.debug%'
